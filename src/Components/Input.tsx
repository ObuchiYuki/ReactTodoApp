import styled from 'styled-components'
import { Colors, FontSize } from "../Const/Const"

export const Input = styled.input`
background: ${Colors.controlBackground};
border-radius: 3px;
color: ${Colors.text};
font-size: 12px;
height: 36px;
padding: 0.25em 1em;
flex-grow: 100;
`